--- a/arch/arm/mach-mediatek/Kconfig
+++ b/arch/arm/mach-mediatek/Kconfig
@@ -10,6 +10,14 @@
 	  Support for Mediatek MT65xx & MT81xx SoCs
 
 if ARCH_MEDIATEK
+
+config Y2_BOOT_DIAGNOSTIC
+	bool "Innioasis Y2 bounded boot diagnostic"
+	depends on !SMP && !THUMB2_KERNEL && !ARM_LPAE && PROC_FS
+	help
+	  Y2-only offline candidate: stop AP watchdog before decompression and
+	  paint a strictly checked inherited LK framebuffer without modesetting.
+	  Do not enable for any other board or ordinary production kernels.
 
 config MACH_MT2701
 	bool "MediaTek MT2701 SoCs support"
